rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: open collaboration on sessions collection.
    match /sessions/{sessionId} {
      allow read, write: if true; // WARNING: Open access for rapid MVP. Tighten before production.
    }
  }
}
